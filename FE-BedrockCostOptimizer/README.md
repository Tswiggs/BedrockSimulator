
  # AWS Bedrock Pricing Tracker

  This is a code bundle for AWS Bedrock Pricing Tracker. The original project is available at https://www.figma.com/design/30j76lUwW4IiNaXKNpCOts/AWS-Bedrock-Pricing-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  